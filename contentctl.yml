#config file
[global]
log_path
log_level
 [content]
detections = uuid1, uuid2, uuid3,

[scheduling]
cron = */24 * * * *

[schedule.fedfad]
cron = */24 * * * *

[actions]
notable = send_email, risk _event

[actions.dasffsda]

[test]
docker_image=””
apps = .
assets = csv

..

[deploy]
target = splunk_search_head, SOAR, git repo 
soar_url = ..
soar_token = dsfadasf

[generate]
format = EnterpriseApp/API/SSA
path = dist/my_app
name = IT

[enrichments]
mitre_enrichment = True
splunk_app = False  [custom_validators]
dsafasf = regex(xxx)
